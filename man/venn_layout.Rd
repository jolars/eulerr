% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{venn_layout}
\alias{venn_layout}
\title{Canonical (non-proportional) Venn layout for a given shape.}
\usage{
venn_layout(set_names, shape)
}
\description{
Returns per-set geometry for a true Venn diagram of \code{set_names.len()} sets,
where every one of the \code{2^n - 1} regions is present. Used by the R \code{venn()}
path for shapes whose layout is supplied by eunoia rather than by eulerr's
precomputed ellipse table. Currently only \code{"rotated_rectangle"} is wired
here (it supports \code{n} in 1..=4); the rotation is what lets four rectangles
open all 15 regions, which axis-aligned rectangles cannot.
}
\keyword{internal}
