% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resolve_fills.R
\name{resolve_region_fills}
\alias{resolve_region_fills}
\title{Resolve per-region fill colors and alphas}
\usage{
resolve_region_fills(fills, dots, id, setnames, n_e, n_id, opar)
}
\arguments{
\item{fills}{the user's \code{fills} argument (already stripped of any \code{by_group}
overrides): \code{TRUE}, a color vector, or a list.}

\item{dots}{additional named styling arguments (from \code{...}).}

\item{id}{the region membership matrix from \code{\link[=build_region_index]{build_region_index()}}.}

\item{setnames}{set names, in \code{id} column order (possibly merged).}

\item{n_e}{number of sets.}

\item{n_id}{number of regions.}

\item{opar}{package options (from \code{\link[=eulerr_options]{eulerr_options()}}).}
}
\value{
a list with \code{gp} (a \code{\link[grid:gpar]{grid::gpar()}} of length \code{n_id}, as consumed by
\code{setup_grobs()}). The resolved per-region fills and alphas are available as
\code{gp$fill} and \code{gp$alpha} for reuse by other renderers.
}
\description{
Normalizes the user's \code{fills} specification into a per-region fill and alpha,
handling named-fill matching in \code{disjoint}/\code{union} mode, length-1/n_sets/
n_subsets expansion, and Lab-space blending of overlaps via \code{\link[=mix_colors]{mix_colors()}}.
}
\keyword{internal}
