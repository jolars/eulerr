% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{apply_panel_overrides}
\alias{apply_panel_overrides}
\title{Overlay per-panel overrides onto a styling parameter}
\usage{
apply_panel_overrides(param, override)
}
\arguments{
\item{param}{a list with \verb{$gp} (a \code{grid::gpar}) and optionally \verb{$rot} and/or
\verb{$set_gp}.}

\item{override}{a flat named list of fields to overlay.}
}
\value{
The param list with overlaid fields.
}
\description{
Used to apply \code{by_group} overrides to \code{fills}, \code{patterns}, \code{edges}, \code{labels},
and \code{quantities} for a single panel produced by \code{euler(..., by = ...)}. The
override list contains gpar-level fields (and optionally \code{rot}), which are
overlaid onto \code{param$gp} (and \code{param$set_gp} for shape-mode patterns).
Structural fields are not overlaid here — they must be filtered upstream.
}
\keyword{internal}
