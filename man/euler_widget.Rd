% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/euler_widget.R
\name{euler_widget}
\alias{euler_widget}
\alias{euler_widget.euler}
\title{Interactive Euler and Venn diagrams}
\usage{
euler_widget(x, ...)

\method{euler_widget}{euler}(
  x,
  fills = TRUE,
  edges = TRUE,
  labels = TRUE,
  quantities = inherits(x, "venn"),
  complement = TRUE,
  n = 200L,
  width = NULL,
  height = NULL,
  elementId = NULL,
  ...
)
}
\arguments{
\item{x}{an object of class \code{euler} or \code{venn}, from \code{\link[=euler]{euler()}} or \code{\link[=venn]{venn()}}.}

\item{...}{additional named graphical parameters forwarded to the fill and
edge styling (a shortcut, as in \code{\link[=plot.euler]{plot.euler()}}).}

\item{fills}{a logical, vector of colors, or list of graphical parameters
for the region fills, following the same rules as the \code{fills} argument of
\code{\link[=plot.euler]{plot.euler()}}.}

\item{edges}{a logical or list controlling the set outlines.}

\item{labels}{a logical or list controlling the set labels.}

\item{quantities}{a logical or list controlling the displayed quantities.
Defaults to \code{TRUE} for \code{venn} objects and \code{FALSE} otherwise, matching
\code{\link[=plot.euler]{plot.euler()}}.}

\item{complement}{a logical controlling whether the container box and
complement count are drawn (only relevant for diagrams fit with
\verb{complement =}).}

\item{n}{number of vertices used to render each shape.}

\item{width, height}{widget dimensions passed to
\code{\link[htmlwidgets:createWidget]{htmlwidgets::createWidget()}}. Usually left \code{NULL} so the widget fills its
container.}

\item{elementId}{an optional string id for the widget root element.}
}
\value{
An object of class \code{htmlwidget} that renders as an interactive SVG
Euler/Venn diagram.
}
\description{
Render a fitted diagram as an interactive HTML widget. Hovering a region
highlights it and shows a tooltip with the region's set combination and
quantity. Unlike \code{\link[=plot.euler]{plot.euler()}}, which draws static \code{grid} graphics, this
backend produces an \link[htmlwidgets:htmlwidget]{htmlwidgets::htmlwidget} suitable for HTML reports,
Shiny apps, and the RStudio Viewer.
}
\details{
The interactive backend depends on \pkg{htmlwidgets} and \pkg{jsonlite},
which are listed under \code{Suggests}. Install them with
\code{install.packages(c("htmlwidgets", "jsonlite"))}.
}
\section{Methods (by class)}{
\itemize{
\item \code{euler_widget(euler)}: Render an \code{euler} or (by inheritance) \code{venn} object.

}}
\examples{
if (requireNamespace("htmlwidgets", quietly = TRUE)) {
  fit <- euler(c(A = 10, B = 5, "A&B" = 3))
  euler_widget(fit)

  # venn objects show quantities by default
  euler_widget(venn(c(A = 10, B = 5, "A&B" = 3)))
}

}
\seealso{
\code{\link[=euler]{euler()}}, \code{\link[=venn]{venn()}}, \code{\link[=plot.euler]{plot.euler()}}
}
