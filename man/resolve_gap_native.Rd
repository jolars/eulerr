% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/label_placement.R
\name{resolve_gap_native}
\alias{resolve_gap_native}
\title{Resolve a \code{gap} option to a numeric in native units inside the current
measurement viewport. \code{NULL} → falls back to \code{padding_native} so the
visible leader-tip gap matches the spacing between label and quantity.
A \code{grid::unit} value converts to native; a bare numeric is interpreted
as \code{lines} (same convention as \code{eulerr_options()$padding}).}
\usage{
resolve_gap_native(gap, padding_native)
}
\arguments{
\item{gap}{the user-supplied gap: \code{NULL}, a \code{\link[grid:unit]{grid::unit()}}, or a bare
numeric in lines}

\item{padding_native}{the fallback used when \code{gap} is \code{NULL}, in native
units}
}
\description{
Resolve a \code{gap} option to a numeric in native units inside the current
measurement viewport. \code{NULL} → falls back to \code{padding_native} so the
visible leader-tip gap matches the spacing between label and quantity.
A \code{grid::unit} value converts to native; a bare numeric is interpreted
as \code{lines} (same convention as \code{eulerr_options()$padding}).
}
\keyword{internal}
