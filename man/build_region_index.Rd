% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resolve_fills.R
\name{build_region_index}
\alias{build_region_index}
\title{Build the region index for a fitted diagram}
\usage{
build_region_index(x)
}
\arguments{
\item{x}{an object of class \code{euler} (or \code{venn}), possibly a faceted list with
a \code{groups} attribute.}
}
\value{
a list with \code{shapes}, \code{n_e} (number of sets), \code{setnames},
\code{combo_labels}, \code{combo_sets}, \code{n_id} (number of regions), and the \code{id}
matrix (\code{id[i, j]} is \code{TRUE} iff region \code{i} includes set \code{j}).
}
\description{
Constructs the working set of regions shared across the plotting pipeline:
singletons (in input order) first, then multi-set combinations in cardinality
and lexicographic order, together with the sparse \code{id} membership matrix.
}
\keyword{internal}
