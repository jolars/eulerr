# Setup gpars

Setup gpars

## Usage

``` r
setup_gpar(default = list(), user = list(), n)
```

## Arguments

- default:

  default values

- user:

  user-inputted values

- n:

  required number of items

## Value

a `gpar` object
