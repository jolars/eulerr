# Check if vector is a real (numeric non-integer)

Check if vector is a real (numeric non-integer)

## Usage

``` r
is_real(x, tol = .Machine$double.eps^0.5)
```

## Arguments

- x:

## Value

A logical.
