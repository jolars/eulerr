# Center ellipses

Center ellipses

## Usage

``` r
center_layout(pars)
```

## Arguments

- pars:

  a matrix or data.frame of x coordinates, y coordinates, minor
  radius (a) and major radius (b)

## Value

A centered version of `pars`.
